description: A display driver that uses a tiff file as framebuffer

compatible: "custom,tiff-display"

include: display-controller.yaml

properties:
  framebuffer_path:
    type: string
    required: true
    description: |
      The path of the framebuffer tiff file.
